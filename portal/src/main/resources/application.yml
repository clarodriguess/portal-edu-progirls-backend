jwt:
  public:
    key: classpath:app.pub
  private:
    key: classpath:app.key

spring:
  application:
    name: portal
  profiles:
    active: dev
  data:
    web:
      pageable:
        default-page-size: 12
        max-page-size: 100

server:
  error:
    include-stacktrace: never